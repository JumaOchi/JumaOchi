
<svg viewBox="0 0 700 400" xmlns="http://www.w3.org/2000/svg" style="background:#1e1e1e;">
  <style>
    .text { fill: #00FF00; font-family: 'Courier New', monospace; font-size: 16px; white-space: pre; }
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
    .cursor { animation: blink 1s step-start infinite; }
    .hide { opacity: 0; }
  </style>

  <!-- Display Skills -->
  <text x="20" y="40" class="text" id="line1">$ cat display_skills.txt</text>
  <text x="40" y="70" class="text" id="line2">Python</text>
  <text x="40" y="95" class="text" id="line3">FastAPI, Django &amp; Flask</text>
  <text x="40" y="120" class="text" id="line4">Typescript</text>
  <text x="40" y="145" class="text" id="line5">Go</text>
  <text x="40" y="170" class="text" id="line6">PostgreSQL, Supabase</text>
  <text x="40" y="195" class="text" id="line7">Power BI, Tableau, Streamlit</text>
  <text x="40" y="220" class="text" id="line8">Docker, Linux, NGINX</text>
  <text x="40" y="245" class="text" id="line9">AWS, Snowflake</text>

  <!-- Clear and Show Other Stuff -->
  <text x="20" y="40" class="text hide" id="line10">$ cat other_stuff.txt</text>
  <text x="40" y="70" class="text hide" id="line11">Fun fact: Debugs better with music 🎧</text>
  <text x="40" y="95" class="text hide" id="line12">Can explain recursion (but only once)</text>
  <text x="40" y="120" class="text hide" id="line13">Plays guitar to relax</text>
  <text x="40" y="145" class="text hide" id="line14">Once fixed a bug by turning it off and on again</text>
  <text x="40" y="170" class="text hide" id="line15">Seeking a team that values growth over perfection</text>

  <!-- Cursor -->
  <text x="20" y="270" class="text cursor">█</text>

  <script type="application/ecmascript"><![CDATA[
    let delay = (ms) => new Promise(res => setTimeout(res, ms));
    async function animate() {
      const skills = [line1,line2,line3,line4,line5,line6,line7,line8,line9];
      const other = [line10,line11,line12,line13,line14,line15];
      while (true) {
        // Show skills
        for (let i = 0; i < skills.length; i++) {
          skills[i].setAttribute("class", "text");
          await delay(400);
        }
        await delay(1500);
        // Hide skills
        for (let i = 0; i < skills.length; i++) {
          skills[i].setAttribute("class", "text hide");
        }
        await delay(500);
        // Show other
        for (let i = 0; i < other.length; i++) {
          other[i].setAttribute("class", "text");
          await delay(400);
        }
        await delay(3000);
        for (let i = 0; i < other.length; i++) {
          other[i].setAttribute("class", "text hide");
        }
        await delay(500);
      }
    }
    animate();
  ]]></script>
</svg>
